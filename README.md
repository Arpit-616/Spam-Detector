# Internship Spam Classification

This repository contains a spam classification notebook and related files for an internship project.

- Notebook: `main.ipynb`
- Data: `spam.csv`

Instructions:
1. Activate the virtual environment: `env\Scripts\Activate.ps1` (PowerShell) or `env\Scripts\activate` (cmd).
2. Install dependencies and run the notebook.
